{% extends 'base.html.twig' %}

{% block title %}Les contacts lors du mission chez ABAT{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    <div class=container">
        <h1>Les contacts de l'agence ABAT</h1>
        <div class="row ">
            <div class="col-md-3 mb-2">
                <h3>Filtrer</h3>

            </div>
            <div class="col-md-9 mb-2">

                <div class="row">
                    {% for contact in contacts %}
                        <div class="card position-relative m-2 " style="width: 18rem;">

                            <div class="card-body">
                                <h5 class="text-center"> Nom: {{ contact.name}} </h5>
                                <p class="price text-center"> <span class="">{{ contact.lastname}}</span> </p>
                                <p class="price text-center"> <span class="">{{ contact.dateNaissance |date("d/m/Y")}}</span> </p>
                                <p class="price text-center"> {{ contact.nationality}} </p>
                                <h5 class="text-center">Mission d'appartenance:</h5>
                                </ul>
                                <h5 class="text-center">Missions:</h5>
                                <ul>
                                    {% for contact in contact.missions %}
                                        <li>{{ contact }}</li>
                                    {% endfor %}
                                </ul>



                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>


        </div>
    </div>
{% endblock %}
